# Linux 轻量性能监控悬浮窗需求文档

## 1. 项目概述

开发一款 Linux 桌面端轻量级性能监控悬浮窗，用于实时显示当前电脑的基础运行状态。

主要显示：

* 下载速度
* 上传速度
* CPU 使用率
* CPU 温度
* 内存使用率
* GPU 使用率
* GPU 温度

软件始终以一个小型悬浮窗口运行在桌面角落，让用户无需打开系统监视器、终端或专业硬件工具，就可以快速知道电脑当前的性能状态。

产品核心原则：

**轻量、简洁、低占用、常驻桌面、一眼看懂。**

---

# 2. 产品定位

Linux 版不是以下软件的替代品：

* htop
* btop
* nvtop
* lm-sensors
* Mission Center
* GNOME System Monitor
* KDE System Monitor

这些工具适合详细分析系统性能。

本工具只解决一个问题：

> 当前电脑现在忙不忙、热不热、网络是否正在大量传输数据。

例如：

```text
↓ 8.2 MB/s  ↑ 520 KB/s
CPU  32%     58°C
RAM  47%
GPU  76%     67°C
```

---

# 3. 支持系统

首期建议支持：

* Ubuntu 22.04+
* Ubuntu 24.04+
* Debian 12+
* Fedora
* Arch Linux
* Manjaro
* Linux Mint
* openSUSE

优先支持：

```text
x86_64
```

后续支持：

```text
ARM64 / aarch64
```

---

# 4. 桌面环境支持

重点支持：

* GNOME
* KDE Plasma
* Cinnamon
* XFCE
* MATE

同时尽量支持：

* Sway
* Hyprland
* Wayfire
* River
* 其他 wlroots Wayland Compositor

需要同时考虑：

```text
X11
Wayland
```

两套桌面显示环境。

---

# 5. 主界面

默认采用无边框悬浮窗口。

推荐默认效果：

```text
┌─────────────────────┐
│ ↓ 8.2M     ↑ 520K   │
│ CPU 32%      58°C   │
│ RAM 47%              │
│ GPU 76%      67°C   │
└─────────────────────┘
```

支持：

* 横向模式
* 纵向模式

---

# 6. 横向模式

适合放置于屏幕顶部或者底部：

```text
↓8.2M ↑520K | CPU 32% 58° | RAM 47% | GPU 76% 67°
```

这是推荐的 Linux 开发 / 办公模式。

例如放置在：

```text
屏幕右上角
```

或者：

```text
顶部面板下方
```

---

# 7. 纵向模式

例如：

```text
↓ 8.2 MB/s
↑ 520 KB/s

CPU 32%  58°C
RAM 47%
GPU 76%  67°C
```

更适合：

* 竖屏
* 多屏
* 大显示器

---

# 8. 网络速度监控

仅监控当前系统整体网络流量。

显示：

```text
↓ Download

↑ Upload
```

例如：

```text
↓ 12.6 MB/s
↑ 680 KB/s
```

不需要：

* 应用级流量
* 进程级流量
* IP 流量
* TCP 连接统计
* UDP 连接统计
* 抓包
* DNS 分析

---

# 9. 网络数据来源

Linux 可以直接读取：

```text
/proc/net/dev
```

获取各网络接口累计：

```text
RX Bytes
TX Bytes
```

计算方式：

```text
下载速度 =
本次 RX Bytes - 上次 RX Bytes
----------------------------
采样时间
```

上传速度：

```text
上传速度 =
本次 TX Bytes - 上次 TX Bytes
----------------------------
采样时间
```

不需要 libpcap。

不需要 root。

不需要抓包权限。

---

# 10. 网络接口处理

Linux 中可能存在大量网络接口，例如：

```text
eth0
enp3s0
wlan0
wlp2s0

docker0

virbr0

vethXXXX

tun0

tailscale0

wg0

br0
```

默认模式：

```text
自动
```

自动统计有效物理网络接口。

设置中允许：

```text
网络接口：

● 自动
○ enp3s0
○ wlp2s0
○ tun0
○ wg0
```

---

# 11. 虚拟网卡过滤

默认建议忽略：

```text
lo

docker*
veth*

virbr*

br-*
```

VPN 网卡：

```text
tun*
tap*
wg*
tailscale*
```

是否统计可以配置。

例如：

```text
统计 VPN 流量

☑ 是
```

---

# 12. CPU 使用率

只显示：

```text
CPU 总使用率
```

例如：

```text
CPU 27%
```

不显示：

* 单核心使用率
* 单线程使用率
* 每个进程使用率

数据可以从：

```text
/proc/stat
```

读取。

通过两次采样计算 CPU 时间差得到系统整体 CPU 使用率。

---

# 13. CPU 温度

Linux 下优先读取：

```text
/sys/class/hwmon/
```

以及：

```text
/sys/class/thermal/
```

可能获取：

```text
CPU Package Temperature
Tctl
Tdie
Core Temperature
```

最终只显示一个代表性 CPU 温度：

```text
CPU 35%  61°C
```

---

# 14. CPU 温度选择规则

Intel 优先：

```text
Package
```

AMD 优先：

```text
Tctl
Tdie
```

如果存在多个 CPU 温度传感器：

选择最适合作为 CPU 整体状态的一个。

不要把所有核心温度显示出来。

---

# 15. 温度不可用处理

如果当前电脑无法读取 CPU 温度：

不要显示：

```text
CPU 35% N/A
```

建议直接变成：

```text
CPU 35%
```

整个监控程序不能因为温度读取失败而报错。

---

# 16. 内存监控

读取：

```text
/proc/meminfo
```

显示：

```text
RAM 48%
```

可以在设置中选择：

```text
RAM 48%

RAM 7.6 / 16 GB

RAM 48% 7.6G
```

默认：

```text
RAM 48%
```

---

# 17. Linux 内存计算

建议按照 Linux 实际可用内存计算。

主要参考：

```text
MemTotal

MemAvailable
```

即：

```text
已使用内存 =
MemTotal - MemAvailable
```

避免简单使用：

```text
MemFree
```

因为 Linux 会大量利用空闲内存作为缓存。

---

# 18. GPU 支持

首期重点支持：

### NVIDIA

```text
GeForce
RTX
GTX
Quadro / RTX Professional
```

### AMD

```text
Radeon
AMD iGPU
```

### Intel

```text
Intel UHD
Intel Iris Xe
Intel Arc
```

---

# 19. NVIDIA GPU

优先通过 NVIDIA 驱动提供的接口获取：

* GPU Usage
* GPU Temperature

可以采用：

```text
NVML
```

而不是不断启动：

```text
nvidia-smi
```

因为持续执行外部进程会增加不必要开销。

如果开发早期为了快速实现，可以先支持 nvidia-smi。

正式版本建议切换到 NVML。

---

# 20. AMD GPU

优先读取：

```text
/sys/class/drm/
```

以及对应：

```text
hwmon
```

获取：

* GPU Busy
* GPU Temperature

例如 AMDGPU 驱动暴露的相关 sysfs 信息。

---

# 21. Intel GPU

Intel GPU 根据驱动环境尝试获取：

* GPU Busy
* Engine Busy
* GPU Temperature

需要兼容：

```text
i915
```

以及后续：

```text
xe
```

驱动。

---

# 22. 多 GPU

例如笔记本存在：

```text
AMD Ryzen iGPU
+
NVIDIA RTX 4060
```

设置页面显示：

```text
GPU

● 自动
○ AMD Radeon Graphics
○ NVIDIA RTX 4060
```

默认：

```text
自动
```

自动优先显示当前有明显负载的 GPU。

---

# 23. 双 GPU 显示

可以提供一个可选功能：

```text
☐ 显示全部 GPU
```

开启以后：

```text
iGPU 12%  51°C
dGPU 86%  72°C
```

但默认关闭。

默认仍然保持：

```text
GPU 86% 72°C
```

保证简洁。

---

# 24. 悬浮窗口

悬浮窗要求：

* 无标题栏
* 无传统窗口边框
* 可拖动
* 始终置顶
* 保存位置
* 不抢输入焦点
* 可鼠标穿透

---

# 25. X11 模式

X11 下应支持真正的：

```text
Always On Top
```

例如通过窗口管理器的 Above 状态。

需要兼容：

* GNOME X11
* KDE X11
* XFCE
* Cinnamon
* MATE

---

# 26. Wayland 模式

Wayland 环境需要单独设计。

不能假设：

```text
X11 的窗口置顶方式
```

在 Wayland 下仍然有效。

程序需要检测当前会话：

```text
XDG_SESSION_TYPE
```

例如：

```text
x11
```

或者：

```text
wayland
```

---

# 27. wlroots Wayland

对于：

* Hyprland
* Sway
* Wayfire
* River

等支持 Layer Shell 的桌面环境，可以使用：

```text
Layer Shell
```

实现真正的悬浮 Overlay。

效果类似：

```text
OSD
桌面面板
性能悬浮层
```

这应该是 Linux Wayland 下最理想的显示方式之一。

---

# 28. GNOME Wayland

GNOME Wayland 需要采用兼容策略。

如果当前桌面环境不支持 Layer Shell：

退化为普通无边框浮动窗口。

必要时提示用户：

```text
当前桌面环境限制应用主动控制窗口层级。

如悬浮层被其他窗口覆盖，可通过桌面环境窗口规则设置置顶。
```

不能因此影响核心监控功能。

---

# 29. KDE Wayland

KDE Plasma 下同样需要处理 Wayland 窗口规则。

程序自身优先实现：

```text
普通浮动窗口
+
Always On Top 请求
```

如果系统不允许应用主动控制，可以让用户通过 KDE：

```text
Window Rules
```

将程序设置为：

```text
Keep Above Other Windows
```

---

# 30. 桌面环境自动识别

启动时读取：

```text
XDG_CURRENT_DESKTOP
DESKTOP_SESSION
XDG_SESSION_TYPE
```

自动判断：

```text
GNOME
KDE
XFCE
Hyprland
Sway
...
```

然后选择最佳 Overlay 实现。

---

# 31. 鼠标穿透

提供：

```text
鼠标穿透
```

开启后：

悬浮窗不能接收鼠标事件。

鼠标可以正常操作悬浮窗后面的：

* IDE
* 浏览器
* 游戏
* Terminal

特别适合作为永久性能状态栏。

---

# 32. 临时关闭鼠标穿透

因为开启穿透后无法点击窗口，所以必须通过：

```text
托盘图标
```

或者：

```text
全局快捷键
```

关闭。

例如：

```text
Ctrl + Alt + M
```

切换：

```text
鼠标穿透 开 / 关
```

快捷键应可配置。

---

# 33. 锁定悬浮窗

提供：

```text
锁定位置
```

锁定后：

* 无法拖动
* 无法调整大小

防止误操作。

---

# 34. 系统托盘

支持：

```text
System Tray
```

托盘菜单：

```text
显示 / 隐藏

锁定位置

鼠标穿透

始终置顶

----------------

设置

----------------

开机自启

----------------

退出
```

---

# 35. GNOME 托盘兼容

部分 GNOME 环境默认不显示传统 Tray Icon。

因此程序不能完全依赖托盘。

建议同时提供：

```text
右键悬浮窗
```

菜单。

例如：

```text
设置
锁定
鼠标穿透
退出
```

---

# 36. 开机自启动

支持：

```text
开机 / 登录后自动启动
```

Linux 推荐使用：

```text
~/.config/autostart/
```

创建：

```text
.desktop
```

启动文件。

例如：

```text
~/.config/autostart/linux-monitor.desktop
```

普通用户即可完成。

不需要 root。

---

# 37. systemd User Service

也可以提供高级模式：

```text
systemd --user
```

但 GUI 程序首选：

```text
XDG Autostart
```

避免把简单桌面程序设计成后台系统服务。

---

# 38. 软件启动行为

提供：

```text
☑ 登录后自动启动

☑ 启动后自动显示悬浮窗
```

默认建议：

```text
自动启动：关闭
自动显示悬浮窗：开启
```

---

# 39. 界面透明度

支持：

```text
40%
50%
60%
70%
80%
90%
100%
```

默认：

```text
85%
```

---

# 40. 背景模式

支持：

```text
深色

浅色

跟随系统

完全透明
```

推荐默认：

```text
深色半透明
```

---

# 41. 无背景模式

例如：

```text
↓3.6M ↑80K | CPU 12% 49° | RAM 42% | GPU 3% 45°
```

完全不显示：

```text
边框
背景
标题栏
```

比较适合开发者桌面。

---

# 42. 字体

建议优先使用系统 UI 字体。

避免程序附带大量字体。

字体大小：

```text
小
中
大
```

或者：

```text
11
12
13
14
16
18
```

---

# 43. DPI / 缩放

Linux 桌面需要支持：

```text
100%

125%

150%

175%

200%
```

以及：

```text
Fractional Scaling
```

悬浮窗不能因为缩放出现：

* 模糊
* 文字截断
* 尺寸错误

---

# 44. 多显示器

支持多个显示器。

例如：

```text
HDMI-1
DP-1
eDP-1
```

用户可以自由把悬浮窗拖到任意显示器。

保存：

```text
Monitor

X

Y
```

---

# 45. 显示器断开处理

例如：

```text
笔记本 + 外接显示器
```

用户把悬浮窗放在外接显示器。

然后拔掉显示器。

程序必须自动检测窗口已经超出当前屏幕区域，并将窗口移动回：

```text
主显示器右上角
```

---

# 46. 刷新频率

默认：

```text
1000ms
```

设置：

```text
500ms

1000ms

2000ms
```

不建议加入：

```text
50ms
100ms
```

因为没有实际必要。

---

# 47. 网速平滑

网络速度可以做轻微平滑。

例如使用：

```text
最近 2～3 次采样
```

避免：

```text
0
15MB
0
20MB
0
```

频繁闪烁。

但必须保留实时感。

---

# 48. 数值颜色

可选：

```text
☑ 高负载颜色提醒
```

例如：

CPU：

```text
<70%      正常

70～89%   较高

≥90%      高
```

GPU：

```text
<80%      正常

80～94%   较高

≥95%      高
```

温度：

```text
<75°C     正常

75～84°C  较高

≥85°C     高
```

颜色提醒默认可以开启，但不要弹通知。

---

# 49. 不需要报警系统

本工具不是服务器监控软件。

不要设计：

```text
CPU 达到 80% 发邮件

温度达到 90°C 发通知

Webhook

Prometheus

Grafana

远程告警
```

保持桌面工具定位。

---

# 50. 设置页面

设置页只分几个类别。

## 常规

```text
开机自启

启动显示悬浮窗

始终置顶

鼠标穿透

锁定窗口
```

## 显示

```text
横向 / 纵向

字体大小

背景

透明度

窗口圆角
```

## 监控

```text
☑ 下载

☑ 上传

☑ CPU 使用率

☑ CPU 温度

☑ RAM

☑ GPU 使用率

☑ GPU 温度
```

## 硬件

```text
网络接口：

自动


GPU：

自动
```

## 高级

```text
刷新频率：

1000ms
```

---

# 51. 数据采集架构

推荐采用统一的数据采集器：

```text
                   Monitor Core

                       │

        ┌──────────────┼──────────────┐

        │              │              │

      CPU            Memory        Network

        │

        ├──────────────┐

        │              │

      GPU          Temperature

                       │

                       ▼

                  Monitor State

                       │

                       ▼

                       UI
```

UI 只负责：

```text
显示
```

不要让 UI 直接读取：

```text
/proc
/sys
```

---

# 52. 数据采集线程

使用一个低频后台任务。

例如：

```text
每 1000ms
```

执行一次：

```text
read_cpu()

read_memory()

read_network()

read_gpu()

read_temperature()
```

然后统一通知 UI 更新。

---

# 53. 避免频繁启动 shell

正式版本不要每秒执行：

```text
cat /proc/stat

free

sensors

nvidia-smi
```

因为这会反复创建 shell / 子进程。

应该直接读取：

```text
/proc

/sys
```

或者调用对应：

```text
Library / API
```

---

# 54. CPU 占用目标

软件空闲运行目标：

```text
< 0.5% CPU
```

大部分时间：

```text
接近 0%
```

---

# 55. 内存目标

建议：

```text
< 100 MB
```

理想情况：

```text
30～70 MB
```

不要为了做一个状态栏启动：

```text
Chromium
Electron
```

---

# 56. 不建议 Electron

首版不建议：

```text
Electron
```

因为产品核心目标就是：

```text
轻量
```

一个只显示几行文字的系统监控程序没有必要附带完整 Chromium Runtime。

---

# 57. 推荐技术栈

Linux 原生版本推荐：

```text
Rust
+
GTK4
```

优势：

* 低资源占用
* 二进制程序
* 性能好
* 内存安全
* Linux 桌面生态成熟
* 很适合读取 procfs/sysfs
* 后续容易支持 Wayland Layer Shell

---

# 58. Wayland Layer Shell

在支持 Layer Shell 的环境下可以考虑：

```text
gtk4-layer-shell
```

或者底层 Wayland Layer Shell 实现。

用于：

```text
Overlay
Top Layer
```

实现真正的桌面悬浮性能窗。

---

# 59. 第二选择

如果开发团队更熟悉 C++：

推荐：

```text
C++
+
Qt 6
```

优势：

* 桌面 UI 成熟
* KDE 兼容性非常好
* 多显示器支持好
* X11 / Wayland 支持成熟

缺点：

Wayland 特殊 Overlay 功能仍然需要针对性处理。

---

# 60. 推荐最终架构

如果目标主要是 Linux：

```text
Rust

GTK4

/proc

/sys

NVML

Wayland Layer Shell
```

这是比较适合该产品定位的一套技术方案。

---

# 61. 配置文件

建议：

```text
~/.config/linux-monitor/config.toml
```

例如：

```text
refresh_interval = 1000

layout = "horizontal"

opacity = 0.85

network = "auto"

gpu = "auto"

always_on_top = true
```

不需要数据库。

---

# 62. 日志

日志默认只记录：

```text
启动异常

传感器初始化失败

GPU 初始化失败

配置错误
```

不要每秒记录：

```text
CPU=22

RAM=46

GPU=50
```

否则产生大量无意义 IO。

---

# 63. 隐私

软件默认：

```text
完全离线运行
```

不需要：

* 注册
* 登录
* 云同步
* 遥测
* 上传硬件信息

网络监控只读取：

```text
统计值
```

不读取：

```text
数据包内容
访问域名
访问 IP
```

---

# 64. 软件包格式

建议正式发布：

```text
AppImage
```

同时可以提供：

```text
.deb

.rpm
```

Arch 用户后续可以提供：

```text
AUR
```

---

# 65. Portable

AppImage 可以作为主要的：

```text
便携版本
```

用户下载以后：

```text
chmod +x
```

即可运行。

尽量避免复杂安装流程。

---

# 66. 权限

核心功能必须做到：

```text
无需 root
```

普通用户即可读取：

* CPU
* RAM
* Network
* 大部分 GPU
* 大部分温度

部分温度传感器没有权限时：

直接跳过。

不要要求用户：

```text
sudo
```

启动整个 GUI。

---

# 67. 睡眠恢复

系统：

```text
Suspend
```

或者：

```text
Hibernate
```

恢复以后：

重新检测：

* 网络接口
* GPU
* hwmon
* 显示器

避免出现：

```text
网速异常暴涨
```

例如睡眠前后的累计流量差值不能直接用于计算实时速度。

恢复后的第一个采样周期应重新作为基准。

---

# 68. 网络变化

例如：

```text
Wi-Fi → Ethernet

Ethernet → Wi-Fi

Wi-Fi → VPN
```

软件自动检测。

不需要用户重启。

---

# 69. GPU 驱动变化

如果 GPU 数据获取失败：

不能导致整个程序退出。

例如：

```text
NVIDIA Driver Reload
```

以后尝试重新连接 NVML。

---

# 70. 默认配置

首次启动：

```text
布局：

横向


位置：

主显示器右上角


主题：

深色


透明度：

85%


刷新率：

1000ms


CPU：

开启


CPU 温度：

开启


RAM：

开启


GPU：

开启


GPU 温度：

开启


下载：

开启


上传：

开启


Always On Top：

开启


鼠标穿透：

关闭


锁定位置：

关闭


开机启动：

关闭
```

---

# 71. 推荐默认效果

Linux 桌面最终效果：

```text
╭────────────────────────────────────────────╮
│ ↓ 3.6M  ↑ 80K │ CPU 12% 49° │ RAM 42% │ GPU 3% 45° │
╰────────────────────────────────────────────╯
```

或者极简无背景：

```text
↓3.6M ↑80K │ CPU 12% 49° │ RAM 42% │ GPU 3% 45°
```

长期放在：

```text
桌面右上角
```

即可。

---

# 72. MVP 第一版

V1.0 必须实现：

1. CPU 总使用率
2. RAM 使用率
3. 下载速度
4. 上传速度
5. CPU 温度
6. NVIDIA GPU 使用率
7. NVIDIA GPU 温度
8. AMD GPU 使用率
9. AMD GPU 温度
10. 横向模式
11. 纵向模式
12. 无边框悬浮窗
13. Always On Top
14. 窗口拖动
15. 窗口位置保存
16. 多显示器
17. 鼠标穿透
18. 锁定窗口
19. X11 支持
20. Wayland 支持
21. 开机自启
22. 深色 / 浅色
23. 透明度
24. 500 / 1000 / 2000ms 刷新
25. 自动检测网络接口

---

# 73. 第二阶段

V1.1 可以增加：

* Intel GPU
* 多 GPU 显示
* 自定义字体
* 自定义文字大小
* 自定义显示顺序
* 自定义间距
* 高负载颜色
* 快捷键
* 全透明模式
* AppImage 自动更新

---

# 74. 明确不开发的功能

为了保持轻量，明确不做：

```text
进程管理器

进程 CPU 排名

进程 RAM 排名

应用网络统计

网络抓包

连接监控

磁盘 SMART

复杂磁盘 IO

风扇控制

超频

降频

电压监控

RGB

硬件跑分

游戏 FPS Hook

复杂历史图表

服务器监控

Prometheus

远程监控

云同步
```

---

# 75. 产品核心

整个 Linux 版本最终应该始终围绕一个场景：

用户正在：

```text
写代码
运行 Docker
编译项目
跑 AI
下载文件
玩游戏
浏览网页
```

只需要抬眼看一下桌面角落：

```text
↓12M ↑1.1M | CPU 87% 76° | RAM 68% | GPU 95% 79°
```

马上知道：

```text
网络有没有在跑

CPU 忙不忙

内存够不够

GPU 有没有工作

机器热不热
```

这就是整个软件最核心的价值。

**不要把它发展成第二个系统任务管理器。**
# 76. 性能与资源占用硬性要求

本项目属于长期常驻型桌面程序，因此：

**程序自身的资源占用属于核心功能指标。**

不能出现：

> 为了监控电脑性能，监控程序自己长期占用明显 CPU 和内存。

性能优化必须从架构阶段开始考虑，而不是开发完成后再进行优化。

---

# 77. 内存占用目标

正式 Release 版本建议设定以下验收目标。

正常运行：

```text
目标：≤ 30 MB RSS
```

理想状态：

```text
15～25 MB RSS
```

可接受上限：

```text
≤ 40 MB RSS
```

如果一个只显示：

```text
网速
CPU
RAM
GPU
温度
```

的悬浮工具长期占用：

```text
80MB
100MB
150MB+
```

则视为不符合本项目的轻量化定位。

---

# 78. CPU 占用目标

软件在：

```text
1000ms 刷新周期
```

下，正常后台运行时：

```text
平均 CPU 占用：

< 0.2%
```

允许短时间采样瞬间出现：

```text
0.5%～1%
```

但不能持续。

理想情况下：

```text
绝大部分时间接近 0%
```

---

# 79. GPU 占用要求

悬浮窗自身原则上：

```text
GPU 占用 ≈ 0%
```

禁止为了显示几行文字使用持续 GPU 动画。

不需要：

* 60 FPS 渲染
* 实时动画
* 模糊动画
* 渐变动画
* 波形动画
* 图表动画

界面只在监控数据发生变化时进行必要重绘。

---

# 80. 磁盘 IO 要求

正常运行过程中：

```text
磁盘持续写入 = 0
```

程序不保存性能历史。

不应该每秒写：

```text
CPU
RAM
GPU
温度
网速
```

日志。

仅在以下情况下写入磁盘：

```text
用户修改设置

窗口位置发生变化并完成拖动

程序出现异常
```

---

# 81. 网络占用要求

软件：

```text
默认完全离线
```

自身不得产生网络流量。

不需要：

```text
服务器

账号

遥测

在线统计

广告

云端配置

后台 API
```

网络模块仅仅读取操作系统提供的：

```text
RX Bytes

TX Bytes
```

统计信息。

---

# 82. 不使用 Electron

明确禁止使用：

```text
Electron
```

原因：

一个只有几行文字的性能悬浮窗没有必要运行：

```text
Chromium
V8
Node.js
```

Electron 的基础内存开销与本项目定位冲突。

---

# 83. 不使用 WebView UI

同样不推荐：

```text
WebView
WebKit
CEF
Tauri WebView
```

虽然部分方案比 Electron 轻，但仍然需要启动完整 Web 渲染环境。

对于本项目：

```text
5～10 行文本
+
简单设置页面
```

没有必要引入浏览器渲染栈。

---

# 84. 技术方案调整

如果把：

```text
低内存
低 CPU
长期常驻
```

作为最高优先级，则技术方案推荐调整为：

```text
Rust

+

Wayland 原生接口

+

X11 原生接口

+

轻量 2D 绘制
```

而不是：

```text
Rust + 完整 GTK4 应用框架
```

GTK4 可以作为开发方便型方案，但不是极致轻量方案。

---

# 85. 推荐最终技术栈

核心：

```text
Rust
```

原因：

* 无 GC
* 内存占用可控
* CPU 开销低
* 单文件程序
* 非常适合读取 procfs / sysfs
* 内存安全
* 适合长期后台运行

---

# 86. Wayland

Wayland 可以考虑：

```text
wayland-client

smithay-client-toolkit
```

对于 Overlay：

```text
wlr-layer-shell
```

从而直接实现：

```text
Overlay Layer
```

避免启动大型 UI Framework。

---

# 87. X11

X11 可以使用：

```text
x11rb
```

或者：

```text
XCB
```

直接创建：

```text
无边框窗口

Always On Top

鼠标穿透窗口
```

不需要完整桌面 UI 框架。

---

# 88. UI 绘制

悬浮主窗口只需要绘制：

```text
背景

文字

少量图标
```

因此推荐使用轻量软件绘制。

例如：

```text
tiny-skia
```

或者类似轻量 2D Renderer。

不需要：

```text
OpenGL 复杂渲染

Vulkan

3D 引擎

游戏 UI 框架
```

---

# 89. 字体渲染

字体建议直接使用系统字体。

可以采用轻量字体栅格化方案。

程序不要内置：

```text
几十 MB 字体资源
```

只需要：

```text
Sans
Monospace
```

类型即可。

默认直接使用系统：

```text
Sans
```

字体。

---

# 90. UI 刷新机制

非常重要：

**不要设计成固定帧率 UI。**

错误方式：

```text
60 FPS

每 16ms redraw()
```

这种方式即使界面完全不变化，也持续消耗：

```text
CPU

GPU
```

正确方式：

```text
数据更新
↓
检查数值是否变化
↓
发生变化
↓
请求一次重绘
```

例如：

```text
CPU：

31%
↓
32%

触发一次 redraw
```

然后重新进入休眠。

---

# 91. 无动画设计

V1.0 禁止加入持续动画。

例如不要：

```text
旋转图标

呼吸灯

实时曲线

数字滚动动画

颜色渐变动画

透明度动画
```

状态颜色变化：

直接切换即可。

---

# 92. 数据采样频率分级

所有数据没有必要使用相同采样周期。

建议：

### CPU

```text
1000ms
```

### RAM

```text
1000ms
```

### 网络

```text
1000ms
```

### GPU Usage

```text
1000ms
```

### CPU Temperature

```text
2000～3000ms
```

### GPU Temperature

```text
2000～3000ms
```

温度没有必要每：

```text
500ms
```

读取一次。

---

# 93. 温度低频读取

例如：

```text
CPU：
每秒读取

RAM：
每秒读取

Network：
每秒读取

GPU Usage：
每秒读取


CPU Temp：
每 3 秒读取

GPU Temp：
每 3 秒读取
```

这样可以进一步降低：

```text
/sys

NVML

hwmon
```

访问次数。

---

# 94. 缓存硬件路径

程序启动以后扫描：

```text
/sys/class/hwmon/
```

找到：

```text
CPU Temperature

GPU Temperature
```

对应路径。

例如：

```text
/sys/class/hwmon/hwmon3/temp1_input
```

缓存下来。

之后直接读取：

```text
temp1_input
```

不要每秒重新遍历整个：

```text
/sys/class/hwmon/
```

目录树。

---

# 95. CPU 数据采集

直接读取：

```text
/proc/stat
```

只解析：

```text
cpu
```

第一行。

例如：

```text
cpu  user nice system idle iowait irq softirq ...
```

不需要解析：

```text
cpu0
cpu1
cpu2
cpu3
...
```

因为本项目只显示：

```text
CPU 总使用率
```

---

# 96. RAM 数据采集

读取：

```text
/proc/meminfo
```

实际上只需要：

```text
MemTotal

MemAvailable
```

没有必要完整解析所有字段。

---

# 97. 网络数据采集

读取：

```text
/proc/net/dev
```

只记录：

```text
RX Bytes

TX Bytes
```

不统计：

```text
Packets

Errors

Dropped

FIFO

Frame

Compressed
```

等无关数据。

---

# 98. 网络接口缓存

首次启动检测网卡。

之后不要每秒重新枚举：

```text
所有网络接口
```

仅在：

```text
网卡消失

网络状态发生变化

恢复休眠
```

时重新检测。

---

# 99. NVIDIA 数据采集

NVIDIA 正式版本推荐：

```text
NVML
```

直接调用库。

不要每秒：

```text
Command::new("nvidia-smi")
```

因为这样会重复：

```text
fork

exec

初始化程序

解析 stdout
```

明显增加 CPU 和内存抖动。

---

# 100. AMD 数据采集

AMD GPU 优先直接读取：

```text
sysfs
```

例如：

```text
/sys/class/drm/cardX/
```

以及：

```text
device/gpu_busy_percent
```

对应 hwmon。

避免启动：

```text
radeontop
```

外部程序。

---

# 101. Intel GPU

同样尽可能：

```text
直接读取 kernel / sysfs 接口
```

不要常驻：

```text
intel_gpu_top
```

作为子进程。

如果某些 Intel GPU 暂时无法轻量获取整体 Usage：

可以选择：

```text
GPU Usage 暂不支持
```

而不是为了兼容它引入高资源占用实现。

---

# 102. 禁止周期性启动外部进程

正式版本不得通过：

```text
每秒执行 shell 命令
```

完成监控。

例如禁止：

```text
sh -c "..."

cat

grep

awk

free

top

sensors

nvidia-smi

radeontop
```

周期执行。

应该：

```text
程序自身直接读取系统接口
```

---

# 103. 不使用 shell

程序运行期间正常情况下：

```text
Shell Process Creation = 0
```

所有读取操作都在当前进程内部完成。

这对于降低：

```text
CPU 抖动

内存抖动

进程创建开销
```

非常重要。

---

# 104. 单进程架构

默认：

```text
单进程
```

即可。

不需要：

```text
monitor-daemon

ui-process

helper-process

renderer-process

background-service
```

组合。

最终：

```text
linux-monitor
```

一个进程即可完成。

---

# 105. 线程数量限制

程序避免创建大量线程。

建议：

```text
主线程
+
1 个采样线程
```

即可。

理想：

```text
2～4 个线程以内
```

不需要：

```text
每个传感器一个线程
```

---

# 106. 统一采样线程

例如：

```text
Monitor Thread
```

每秒唤醒一次：

```text
CPU

Memory

Network

GPU
```

温度使用计数器：

```text
第 1 秒：CPU + RAM + NET + GPU

第 2 秒：CPU + RAM + NET + GPU

第 3 秒：
CPU + RAM + NET + GPU
+
CPU Temp
+
GPU Temp
```

这样避免建立多个：

```text
Timer
Thread
Task
```

---

# 107. 事件驱动 UI

架构：

```text
                ┌───────────────┐
                │ Monitor Thread │
                └───────┬───────┘
                        │
                        ▼
                 MonitorState
                        │
                  数据发生变化
                        │
                        ▼
                    UI Event
                        │
                        ▼
                     Redraw
```

其余时间：

```text
UI Thread Sleep
```

---

# 108. 数值变化阈值

甚至可以避免无意义重绘。

例如 CPU：

```text
31.1%
31.2%
31.3%
```

界面最终都显示：

```text
31%
```

那么：

```text
不需要重绘
```

只有：

```text
31% → 32%
```

时才更新。

---

# 109. 网速显示变化

例如内部：

```text
5.212 MB/s
```

界面：

```text
5.2M
```

只有最终格式化后的显示值发生变化：

才请求重绘。

---

# 110. 无历史数据库

明确禁止：

```text
SQLite

LevelDB

RocksDB

历史监控数据库
```

本工具不保存：

```text
过去 24 小时 CPU

过去一周温度

流量历史
```

这样既减少：

```text
内存
磁盘 IO
代码复杂度
```

也保持产品定位。

---

# 111. 配置文件

只使用一个非常小的配置：

```text
~/.config/linux-monitor/config.toml
```

通常：

```text
< 10KB
```

程序启动时：

```text
读取一次
```

修改设置时：

```text
写入一次
```

---

# 112. 日志策略

Release 默认：

```text
不开启普通运行日志
```

仅发生严重错误时写入日志。

例如：

```text
GPU 初始化失败

配置损坏

窗口创建失败
```

日志设置最大：

```text
1MB
```

超过后覆盖。

---

# 113. Debug 与 Release 分离

开发版本可以开启：

```text
Trace

Debug Log

Sensor Debug
```

但 Release：

必须全部关闭。

不能把开发调试日志带入正式版导致：

```text
磁盘不断写入

CPU 增加
```

---

# 114. 悬浮窗极简模式

默认界面只存在：

```text
背景矩形

1～4 行文字
```

例如：

```text
↓3.6M ↑80K │ CPU 12% 49° │ RAM 42% │ GPU 3% 45°
```

不需要：

```text
复杂 Widget Tree

ScrollView

Web DOM

大量 Layout Container
```

---

# 115. 设置页面按需加载

设置页面平时：

```text
不存在
```

用户点击：

```text
Settings
```

才创建设置窗口。

关闭设置后：

```text
释放设置 UI
```

不要让完整设置界面长期常驻内存。

---

# 116. Tray 按需实现

系统托盘只负责：

```text
显示 / 隐藏

鼠标穿透

锁定

设置

退出
```

不要加入复杂菜单动画和图标资源。

---

# 117. 图片资源限制

整个软件尽量：

```text
0 图片资源
```

或者只包含：

```text
一个 SVG Logo
```

图标尽可能使用：

```text
简单矢量路径

Unicode Symbol
```

从而减少程序包体和运行时资源。

---

# 118. Release 编译优化

Rust Release 建议开启：

```text
opt-level = "z"

lto = true

codegen-units = 1

panic = "abort"

strip = true
```

根据性能测试，也可以使用：

```text
opt-level = 3
```

对比测试。

重点不是单纯追求最小文件：

而是同时验证：

```text
程序大小

CPU

RSS
```

---

# 119. 性能基准测试

每个 Release 都必须进行资源基准测试。

测试机器至少包含：

```text
Intel + NVIDIA

AMD CPU + AMD GPU

Intel iGPU

笔记本双 GPU
```

测试时间：

```text
至少连续运行 8 小时
```

检查：

```text
内存是否增长

CPU 是否异常

文件描述符是否泄漏

线程数量是否增长
```

---

# 120. 内存泄漏验收

程序启动：

```text
RSS 20MB
```

运行几个小时以后不能变成：

```text
40MB
80MB
150MB
```

允许存在少量：

```text
allocator

字体缓存

图形缓存
```

初始化增长。

但进入稳定状态后：

```text
内存必须基本稳定
```

---

# 121. 24 小时压力测试

建议发布前执行：

```text
24 小时常驻测试
```

期间进行：

```text
Wi-Fi 切换

VPN 开关

显示器插拔

Suspend / Resume

NVIDIA GPU 空闲 / 工作

全屏程序

桌面切换
```

确认软件：

```text
不会持续增长内存

不会产生僵尸进程

不会大量写磁盘

不会持续消耗 CPU
```

---

# 122. 推荐性能预算

整个程序可以设定如下性能预算：

```text
RSS：

目标 ≤ 30MB


CPU：

长期平均 < 0.2%


GPU：

接近 0%


线程：

≤ 4


正常磁盘写入：

0 B/s


自身网络：

0 B/s


子进程：

0


UI 刷新：

约 1 次/秒
且只在需要时刷新
```

---

# 123. 开发优先级

项目开发过程中优先级应固定为：

```text
1. 稳定

2. 低资源占用

3. 数据准确

4. X11 / Wayland 兼容

5. UI 简洁

6. 高级功能
```

如果一个功能明显增加：

```text
30～50MB 内存

持续 CPU 消耗

复杂依赖
```

则原则上：

**不加入。**

---

# 124. 最终推荐架构

最终建议：

```text
                Linux Monitor

                     │
              Rust Single Process
                     │
       ┌─────────────┴──────────────┐
       │                            │
   Monitor Core                Window Layer
       │                            │
       │                       ┌────┴────┐
       │                       │         │
       │                   Wayland     X11
       │                       │         │
       │                 Layer Shell   x11rb
       │
 ┌─────┼─────┬──────┬──────┐
 │     │     │      │      │
CPU   RAM   NET    GPU    TEMP
 │     │     │      │      │
proc  proc  proc   sysfs  hwmon
                  NVML
```

原则：

```text
不启动浏览器

不启动 Shell

不启动外部监控软件

不运行数据库

不保存历史

不持续写日志

不使用固定 FPS 渲染
```

---

# 125. 产品最终性能目标

用户运行：

```text
btop
```

或者：

```text
htop
```

查看系统时，本程序自身不应该成为一个明显的资源使用者。

理想效果：

```text
linux-monitor

CPU     0.0%
MEM     20～30MB
```

偶尔刷新：

```text
CPU 0.1%～0.3%
```

随后重新回到：

```text
0.0%
```

这才符合本项目：

**“轻量性能监控悬浮窗”**

的产品定位。
